既然你已经踏入了 Emacs 的世界，那我们就直接进入“指法练习”模式。Emacs 的快捷键逻辑其实很清晰：**C (Control)** 通常用于基本操作，**M (Meta/Alt)** 通常用于更高级或以“词”为单位的操作。

以下是整理好的常用快捷键，特别针对你要求的**切屏**和**删除**进行了加固。

---

## 1. 屏幕与窗口管理 (Window Management)

在 Emacs 中，你看到的“窗口”其实是 **Window**，而整个程序大窗口叫 **Frame**。

|**快捷键**|**功能描述**|
|---|---|
|**C-x 2**|水平分割窗口（上下分屏）|
|**C-x 3**|垂直分割窗口（左右分屏）|
|**C-x o**|(Other) 光标跳转到下一个窗口|
|**C-x 0**|关闭**当前**光标所在的窗口|
|**C-x 1**|关闭**其它**所有窗口，只留当前这一个|
|**C-x ^**|加高当前窗口|
|**C-x { / }**|收缩或拉宽当前窗口|

---

## 2. 删除与剪切 (Deleting & Killing)

Emacs 区分“删除”（不进剪贴板）和“剪切/干掉”（Kill，进入剪贴板/Kill Ring）。

|**快捷键**|**功能描述**|
|---|---|
|**C-d**|删除光标后一个字符（Delete）|
|**M-d**|删除光标后的**一个单词**|
|**C-k**|(Kill) 从光标处剪切到**行末**|
|**M-k**|剪切到句末|
|**C-w**|剪切选中的区域（Region）|
|**M-w**|复制选中的区域（不删除）|
|**C-y**|(Yank) 粘贴最后一次剪切的内容|
|**M-y**|在粘贴后按，可以循环选择之前的剪切记录|

---

## 3. 基础移动 (Navigation)

如果你不想用方向键，这些是老手的标配：

- **C-f / C-b**: 前进 / 后退一个字符 (Forward / Backward)
    
- **M-f / M-b**: 前进 / 后退一个**单词**
    
- **C-p / C-n**: 上一行 / 下一行 (Previous / Next)
    
- **C-a / C-e**: 行首 / 行尾
    
- **M-a / M-e**: 句首 / 句尾
    
- **M-< / M->**: 整个文档的最开头 / 最末尾
    

---

## 4. 文件与系统 (Files & Buffer)

- **C-x C-f**: 打开/新建文件 (Find file)
    
- **C-x C-s**: 保存文件 (Save)
    
- **C-x b**: 切换缓冲区 (Buffer)
    
- **C-x k**: 关闭当前缓冲区 (Kill buffer)
    
- **C-x C-c**: 退出 Emacs
    

---

## 5. 救命用的 (The Essentials)

- **C-g**: **最重要的键！** 取消当前正在输入的命令或摆脱卡死状态。
    
- **C-x u**: 撤销 (Undo)。
    
- **M-x**: 输入命令名来执行功能（当你忘记快捷键时，直接搜名字）。
    

> **小贴士：**
> 
> 如果你觉得 `C-x o` 在多个窗口间切来切去太慢，建议在配置文件里开启 `windmove`，这样就可以用 `Shift + 方向键` 直接跳窗了。