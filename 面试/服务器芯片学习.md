Bootloader -->
BootRom 执行第一行代码，选介质，安全校验，加载下一级
SCP 独立于CUP的处理单元，负责供电时钟散热等内容
U-Boot 初始化内存，驱动基础外设，加载linux镜像
Linux Kernel 管理资源，驱动所有硬件
### 通信总线学习
半双工：一根线，只能一个发消息，另一个要等着
全双工：两条线，可以同时
**uart**
空闲是高电平
需要发一个低电平的起始位，然后发5-8位的数据位，然后发一个校验位，最后发送停止位（1.-2位）
先发低位，再发高位
移位器shifter，寄存器register，时钟源，波特率控制器，控制单元
IIC
两根信号线，一根SDA用于收发数据，一根SCL用于时钟同步
半双工
多主机总线：主机可以进行呼叫，IIC具备冲突检测和仲裁的功能
每个器件都是唯一地址
- 主机发送起始信号，启用总线
- 发送一个字节指明从机地址和传输方向，最低位0是主到从
- 从机发送应答信号
- 发送器发一个字节数据，接收器发送应答信号回应发送器
- 如上所示
- 完成以后主机发送停止信号给所有
是在SCL为高的时候，起始信号SDA有高变低
每次都是八位，先发高位，接收器发一位应答--主机和从机的SDA
应答信号是接收器的SDA由高变低，也是在SCL低电平的时候
SCL为低电平的时候，发送器改变数据，低电平，接收器接收数据
九个时钟变化
双机通信，中间不需要发停止信号
