## 一、C++ 基础与 C 的区别

### 1. 注释

- C 风格：`/* 多行注释 */`，C++ 兼容
- C++ 风格：`// 单行注释`，推荐日常使用，简洁高效

### 2. 输入输出

C++ 推荐**流操作**替代 C 的`scanf/printf`，包含头文件`<iostream.h>`，核心流对象：

表格

|流对象|作用|操作符|
|---|---|---|
|cout|标准输出|<<（输出）|
|cin|标准输入|>>（输入）|
|cerr|标准错误（无缓冲）|<<|
|clog|标准错误（有缓冲）|<<|

- 优势：编译期类型检查、速度更快、支持用户自定义类型重载
- 示例：`cout << "值：" << a << endl; cin >> b;`

### 3. 变量与常量

- **变量声明**：C++ 可在代码任意位置声明，就近声明提升可读性（如`for`循环内）
    
    - 示例：`for(int i=0; i<10; i++)`（i 仅在循环内有效）
    
- **常量**：用`const`替代 C 的`#define`，类型安全且有作用域
    
    - 示例：`const int N=10; int arr[2*N];`（C++ 允许，C 禁止）
    
- **常量与指针**：区分 “指向常量的指针” 和 “常量指针”
    
    c++
    
    ```
    const char *p1; // 指向常量，p1可改，*p1不可改
    char *const p2; // 指针常量，p2不可改，*p2可改
    const char *const p3; // 均不可改
    ```
    

### 4. 引用（C++ 独有）

- 定义：`类型 &引用名 = 原变量;`，**必须初始化**，是原变量的 “别名”
- 特性：修改引用即修改原变量，大小与原变量一致，不能为 NULL
- 用途：函数参数传递（替代指针，简化代码）、函数返回值
- 示例：
    
    c++
    
    ```
    int a=1; int &ra=a; ra=2; // a的值变为2
    void swap(int &x, int &y) {int t=x;x=y;y=t;} // 直接交换原变量
    ```
    

### 5. 内存分配

C++ 用**new/delete**替代 C 的`malloc/free`，自动调用构造 / 析构函数，类型安全：

表格

|操作|C++ 语法|说明|
|---|---|---|
|单个变量分配|`int *p = new int(10);`|分配并初始化值为 10|
|数组分配|`int *arr = new int[5];`|分配 5 个 int 的数组|
|单个变量释放|`delete p;`|对应单个 new|
|数组释放|`delete[] arr;`|对应数组 new，必须加 []|

- 分配失败：抛出`bad_alloc`异常，可通过`set_new_handler`设置自定义错误处理函数

### 6. 复合类型

- 结构体 / 枚举：C++ 无需`typedef`即可直接用类型名定义变量
    
    c++
    
    ```
    struct Fiche {char *nom; int age;};
    Fiche p; // C++直接用，C需要typedef Fiche Fiche;
    enum Bool {FAUX, VRAI};
    Bool b=FAUX; // C++类型检查严格，不能直接赋值为0
    ```
    

## 二、C++ 函数进阶

### 1. 函数声明

- C++ 强制**原型声明**，`int f();`等价于`int f(void);`（无参数），与 C 不同（C 中表示参数任意）
- 非`void`函数必须有返回值，编译期检查更严格

### 2. 参数默认值

- 语法：`返回值 函数名(类型 参数1, 类型 参数2=默认值);`
- 规则：**默认参数必须放在参数列表末尾**，仅在原型中声明，定义中不写
- 示例：`void print(int a, int base=10); print(16);`（默认按 10 进制输出）

### 3. 内联函数（inline）

- 语法：`inline 返回值 函数名(参数) {函数体;}`
- 作用：替代 C 的`#define`宏函数，**编译期将函数代码嵌入调用处**，提升执行速度
- 特性：有类型检查，仅适用于**短函数**（几行代码），作用域限于当前模块

### 4. 函数重载

- 定义：**同名函数**，但**参数列表（个数 / 类型 / 顺序）不同**（返回值不同不构成重载）
- 编译器依据**函数签名**（函数名 + 参数列表）匹配调用
- 示例：
    
    c++
    
    ```
    int sum(int a, int b);
    double sum(double a, double b);
    int sum(int a, int b, int c); // 均为重载，合法
    ```
    
- 注意：`char*`和`char[80]`视为同一类型，`const`参数仅在引用 / 指针下构成重载

### 5. 函数返回引用

- 特性：返回引用可作为**左值**，但返回的变量必须有**持久的生命周期**（如全局变量、类成员）
- 示例：
    
    c++
    
    ```
    int t[20];
    int &nth(int i) {return t[i];}
    nth(0)=10; // 合法，直接修改t[0]
    ```
    

### 6. 调用 C 语言函数

- C++ 编译会对函数名进行**名字改编**，调用 C 函数需用`extern "C"`声明
- 示例：
    
    c++
    
    ```
    extern "C" {
        int f1(int a);
        void f2(char *s);
    }
    ```
    

## 三、类与对象（核心）

### 1. 类的基本定义

- 语法：`class 类名 { 访问控制符: 成员变量/成员函数; };`（末尾必须加；）
- 访问控制符（核心，实现封装）：
    
    表格
    
    |控制符|访问范围|
    |---|---|
    |public|所有地方可访问（类的接口）|
    |private|仅类内部 / 友元可访问（默认，类的实现）|
    |protected|类内部 / 友元 / 派生类可访问（用于继承）|
    
- 结构体与类的区别：`struct`默认**public**，`class`默认**private**，其余功能一致

### 2. 成员函数

- **内联成员函数**：类内定义的函数自动为 inline，类外定义需显式加`inline`
- **类外定义语法**：`返回值 类名::成员函数名(参数) {函数体;}`
    
    示例：`void Avion::affiche() {cout << _poids << endl;}`
- **常量成员函数**：函数后加`const`，**不能修改类的成员变量**，可被常量对象调用
    
    示例：`int getn() const {return _n;}`
- **重载常量成员函数**：const 与非 const 版本可重载，区分常量 / 非常量对象调用
    
    c++
    
    ```
    char &nth(int i); // 非常量对象调用
    char nth(int i) const; // 常量对象调用
    ```
    

### 3. 对象的创建与使用

- **实例化**：`类名 对象名;`（栈）、`类名 *p = new 类名;`（堆，需 delete 释放）
- **成员访问**：
    
    - 栈对象 / 引用：`对象名.成员`（如`av1.init();`）
    - 堆对象 / 指针：`对象名->成员`（如`av2->affiche();`）
    

### 4. 构造函数

- 作用：**对象创建时自动调用**，初始化成员变量，无返回值，**函数名与类名完全一致**
- **默认构造函数**：无参数或所有参数都有默认值，若用户未定义，编译器自动生成
- **带参构造函数**：支持重载，实现多方式初始化
- **拷贝构造函数**：参数为`const 类名 &`，用于**对象拷贝 / 赋值 / 函数传参 / 返回值**，用户未定义则编译器自动生成（浅拷贝）
    
    - 语法：`类名(const 类名 &obj) {拷贝逻辑;}`
    - 深拷贝：针对有动态内存的类，需手动实现（避免浅拷贝导致的内存泄漏）
    
- **初始化列表**：替代构造函数内赋值，**必须用于初始化 const 成员 / 引用成员 / 类类型成员**
    
    - 语法：`类名::类名(参数) : 成员1(值1), 成员2(值2) {函数体;}`
    - 示例：`X::X(int a) : _x(a), _y(0) {}`
    

### 5. 析构函数

- 作用：**对象销毁时自动调用**，释放动态内存等资源，无返回值，无参数，**不能重载**
- 语法：`~类名() {释放逻辑;}`
- 若类有动态内存，**必须手动实现析构函数**，否则编译器生成的默认析构会导致内存泄漏

### 6. this 指针（C++ 独有）

- 定义：每个非静态成员函数都有一个**隐藏的 const 指针**，指向当前对象，语法：`类名 *const this`
- 作用：区分成员变量与局部变量，返回当前对象（实现链式调用）
- 示例：
    
    c++
    
    ```
    class X {int _i; public: int geti() {return this->_i;} X& f() {return *this;}};
    X x; x.f().f(); // 链式调用，this指向x
    ```
    

### 7. 静态成员（static）

- **静态成员变量**：所有对象**共享**，属于类而非对象，**必须在类外初始化**
    
    - 示例：`class A {static int n;}; int A::n=0;`
    
- **静态成员函数**：属于类，无 this 指针，**只能访问静态成员**，可直接通过类名调用
    
    - 示例：`A::print();`（无需创建对象）
    
- 用途：统计类的对象个数、实现全局共享的常量 / 函数

### 8. 友元（friend）

- 定义：突破封装，允许**友元函数 / 友元类**访问类的 private/protected 成员
- 语法：
    
    - 友元函数：`class 类名 {friend 返回值 函数名(参数);};`
    - 友元类：`class 类名 {friend class 友元类名;};`
    
- 注意：友元关系**不可传递**、**不可继承**，仅为效率使用，避免滥用（破坏封装）

### 9. 类模板（泛型编程）

- 作用：定义**通用的类 / 函数**，支持任意类型，实现代码复用
- 语法：`template <class T> class 类名 { T 成员; };`（T 为类型参数，可替换为任意类型）
- 示例：
    
    c++
    
    ```
    template <class T>
    class Pile { T *arr; public: void push(T val); };
    Pile<int> p1; // 实例化为int类型的栈
    Pile<double> p2; // 实例化为double类型的栈
    ```
    

## 四、运算符重载

### 1. 基本规则

- 定义：为用户自定义类型（如类）重新定义运算符的行为，语法：`返回值 operator运算符(参数) {逻辑;}`
- 可重载：`+、-、*、/、==、!=、<<、>>`等（大部分运算符）
- 不可重载：`::、.、.*、?:、sizeof`
- 特性：**不改变运算符的优先级 / 结合性 / 操作数个数**，不能创建新运算符

### 2. 重载方式

#### （1）类成员函数重载

- 语法：`class 类名 { 返回值 operator运算符(参数) {逻辑;} };`
- 隐含 this 指针，**操作数个数比运算符实际少 1**（左操作数为当前对象）
- 适用于：单目运算符、赋值运算符（=）、复合赋值（+=、-=）等
- 示例：`Matrice operator+(const Matrice &m) {求和逻辑;}`

#### （2）全局函数重载

- 语法：`返回值 operator运算符(左操作数, 右操作数) {逻辑;}`
- 需将函数声明为类的友元（若访问私有成员）
- 适用于：双目运算符、流操作符（<<、>>）等（支持左操作数为内置类型）
- 示例：
    
    c++
    
    ```
    class Nombre {friend Nombre operator+(const Nombre &a, const Nombre &b);};
    Nombre operator+(const Nombre &a, const Nombre &b) {return Nombre(a._n+b._n);}
    ```
    

### 3. 常用运算符重载

#### （1）流操作符（<<、>>）

- 必须用**全局函数 + 友元**实现，返回流对象引用以支持链式操作
- 示例：
    
    c++
    
    ```
    class Ex {friend ostream &operator<<(ostream &os, const Ex &e);};
    ostream &operator<<(ostream &os, const Ex &e) {
        os << e._nom << " " << e._valeur;
        return os;
    }
    ```
    

#### （2）赋值运算符（=）

- 必须用**类成员函数**实现，需处理**自赋值**，返回 * this 以支持链式赋值
- 深拷贝：针对动态内存，手动实现释放旧内存、分配新内存、拷贝数据
- 示例：
    
    c++
    
    ```
    Classe &operator=(const Classe &obj) {
        if(this == &obj) return *this; // 自赋值判断
        delete[] _p; // 释放旧内存
        _p = new int[obj._n]; // 分配新内存
        for(int i=0; i<obj._n; i++) _p[i]=obj._p[i];
        return *this;
    }
    ```
    

#### （3）自增 / 自减运算符（++、--）

- 区分**前缀（++a）**和**后缀（a++）**，后缀加**int 哑元**区分
- 示例：
    
    c++
    
    ```
    class A {
        int n;
        public:
            A& operator++() {n++; return *this;} // 前缀，返回引用
            A operator++(int) {A t=*this; n++; return t;} // 后缀，返回值
    };
    ```
    

#### （4）关系运算符（==、!=）

- 建议用全局函数实现，支持对称调用（如内置类型与自定义类型比较）
- 示例：`bool operator==(const Nombre &a, const Nombre &b) {return a._n==b._n;}`

## 五、继承与多态

### 1. 继承的基本概念

- 定义：从**基类（父类）**派生出**派生类（子类）**，派生类继承基类的非 private 成员，实现代码复用
- 语法：`class 派生类名: 继承方式 基类名 {派生类成员;};`
- 继承方式（决定基类成员在派生类中的访问权限）：
    
    表格
    
    |继承方式|基类 public|基类 protected|基类 private|
    |---|---|---|---|
    |public|public|protected|不可访问|
    |protected|protected|protected|不可访问|
    |private|private|private|不可访问|
    
- 注：private 成员始终不可被派生类访问，默认继承方式：class 为 private，struct 为 public

### 2. 派生类的构造与析构

- **构造函数**：派生类构造时，**先调用基类构造函数**，再调用派生类构造函数；若基类无默认构造，需用**初始化列表**显式调用
    
    - 示例：`Employe::Employe(char *n) : Salarie(n) {派生类初始化;}`
    
- **析构函数**：派生类析构时，**先调用派生类析构函数**，再调用基类析构函数（与构造相反）
- 注意：构造 / 析构 / 赋值运算符**不能被继承**，需派生类手动实现

### 3. 成员重定义与作用域解析

- 派生类可重定义基类的成员函数（同名），调用时默认调用派生类版本
- 调用基类版本：用**作用域解析符::**，示例：`Salarie::affiche();`

### 4. 多重继承

- 定义：一个派生类继承**多个基类**，语法：`class C: public A, public B {成员;};`
- 问题：**菱形继承**（多个基类继承同一基类，导致派生类有多个基类实例）
- 解决：**虚继承**，在基类继承时加`virtual`，使派生类仅保留一个基类实例
    
    - 语法：`class B: virtual public A {}; class C: virtual public A {}; class D: public B, public C {};`
    

### 5. 多态（核心，基于继承）

#### （1）概念

- 定义：同一操作作用于不同对象，产生不同的行为，分为**静态多态**（编译期，如函数重载）和**动态多态**（运行期，基于虚函数）
- 动态多态条件：① 继承关系 ② 基类指针 / 引用指向派生类对象 ③ 基类成员函数被声明为**虚函数**

#### （2）虚函数

- 语法：基类函数前加`virtual`，派生类重定义时可加可不加（自动继承虚属性）
- 作用：**运行期绑定**（晚绑定），根据指针 / 引用实际指向的对象类型，调用对应的函数版本
- 示例：
    
    c++
    
    ```
    class Forme {public: virtual void dessiner() {cout << "Forme";}};
    class Cercle: public Forme {public: void dessiner() {cout << "Cercle";}};
    Forme *p = new Cercle;
    p->dessiner(); // 调用Cercle::dessiner()，动态多态
    ```
    

#### （3）纯虚函数与抽象类

- 纯虚函数：`virtual 返回值 函数名(参数) = 0;`，无函数体，强制派生类重定义
- 抽象类：包含至少一个纯虚函数的类，**不能实例化对象**，仅能作为基类使用
- 用途：定义接口，统一派生类的行为，示例：`class Forme {public: virtual void dessiner()=0;};`

#### （4）虚析构函数

- 作用：当基类指针指向派生类对象并释放时，**保证派生类析构函数被调用**，避免内存泄漏
- 语法：基类析构函数前加`virtual`，示例：`virtual ~Forme() {};`
- 注意：构造函数不能是虚函数，静态成员函数不能是虚函数

## 六、输入输出流（IO 流）

### 1. 流的层次结构

- 基类：`ios`（管理流状态、格式控制）
- 输入流：`istream`（继承 ios，如 cin）
- 输出流：`ostream`（继承 ios，如 cout）
- 双向流：`iostream`（继承 istream+ostream）
- 文件流：包含头文件`<fstream.h>`，`ifstream`（文件输入）、`ofstream`（文件输出）、`fstream`（文件双向）

### 2. 文件操作

#### （1）文件打开

- 方式 1：构造函数直接打开：`ofstream fout("test.txt");`
- 方式 2：`open`函数打开：`ifstream fin; fin.open("test.txt", 打开模式);`
- 打开模式（来自 ios 类，可组合使用，用 |）：
    
    表格
    
    |模式|作用|
    |---|---|
    |ios::in|读模式（ifstream 默认）|
    |ios::out|写模式（ofstream 默认，覆盖原有内容）|
    |ios::app|追加模式，写在文件末尾|
    |ios::binary|二进制模式（默认文本模式）|
    |ios::trunc|清空文件（与 ios::out 配合）|
    

#### （2）文件读写

- **文本模式**：用`<<`（写）、`>>`（读），与控制台流一致
- **二进制模式**：用`write`（写）、`read`（读），需强制类型转换
    
    - 示例：`fout.write((char*)&a, sizeof(a)); fin.read((char*)&a, sizeof(a));`
    

#### （3）文件定位

- 输出流定位：`tellp()`（返回当前写位置）、`seekp(位置, 参照点)`
- 输入流定位：`tellg()`（返回当前读位置）、`seekg(位置, 参照点)`
- 参照点：`ios::beg`（文件开头）、`ios::cur`（当前位置）、`ios::end`（文件末尾）
- 示例：`fin.seekg(0, ios::end);`（定位到文件末尾）

#### （4）流状态检查

- 核心函数（来自 ios 类）：
    
    - `good()`：流状态正常，返回 true
    - `eof()`：到达文件末尾，返回 true
    - `fail()`：操作失败（如格式错误），返回 true
    - `bad()`：严重错误（如文件损坏），返回 true
    - `clear()`：重置流状态，用于错误后恢复
    

### 3. 格式控制

#### （1）格式标志

通过`setf()`设置，`unsetf()`清除，示例：`cout.setf(ios::hex, ios::basefield);`（设置十六进制）

常用标志：`ios::dec`（十进制）、`ios::hex`（十六进制）、`ios::left`（左对齐）、`ios::fixed`（固定小数）

#### （2）操纵符

包含头文件`<iomanip.h>`，简化格式控制，可直接嵌入流操作

常用操纵符：

表格

|操纵符|作用|
|---|---|
|endl|换行并刷新缓冲区|
|hex/dec/oct|十六 / 十 / 八进制|
|setw(n)|设置字段宽度 n（仅对下一个输出有效）|
|setprecision(n)|设置小数精度 n|
|setfill(c)|设置填充字符 c|
|flush|刷新缓冲区|

- 示例：`cout << setw(8) << setfill('*') << hex << 123 << endl;`

## 七、核心编程规范与易错点

### 1. 封装原则

- 成员变量私有化（private），通过公共成员函数（public）访问 / 修改（get/set）
- 避免直接暴露成员变量，保证数据安全性和可维护性

### 2. 继承与多态易错点

- 基类定义虚函数时，务必将析构函数声明为虚函数
- 纯虚函数的派生类必须重定义所有纯虚函数，否则仍为抽象类
- 多重继承尽量避免，必须使用时用虚继承解决菱形问题

### 3. 动态内存管理

- 有动态内存的类，必须手动实现**拷贝构造函数、赋值运算符、析构函数**（三法则）
- `new[]`必须与`delete[]`配对，避免内存泄漏 / 野指针
- 避免返回局部变量的引用（生命周期结束，引用失效）

### 4. 友元的使用

- 仅为提升效率使用（如流操作符、运算符重载），避免滥用破坏封装
- 友元关系不可传递、不可继承，无需在派生类中重新声明

### 5. 常量的使用

- 用`const`替代`#define`，类型安全且有作用域
- 常量成员函数、常量对象仅能访问常量成员 / 常量成员函数

### 6. 函数重载与重定义的区别

- **重载**：同一作用域，同名函数，参数列表不同
- **重定义**：不同作用域（基类与派生类），同名函数，参数列表可同可不同（覆盖基类函数）